const points = document.getElementById("points");
const startbutton = document.getElementById("startbutton");
const lo = document.getElementById ("loGather");
const apple = document.getElementById("apples");

var gameover =setInterval(funtion()){
    var loTop = parseInt(window.getComputedStyle(loGather).getPropertyValue("top"));
}